/**
 * @return {string}
 */
function DivisionStringified(num1,num2) {
    const divide = Math.round((num1 / num2).toString());

    return divide.toLocaleString();

    //* shows the number with a comma separator
}

